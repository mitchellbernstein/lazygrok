---
name: debug
description: "[LazyGrok] Alias for OmO debugging skill. Use when user types debug, /debug, or wants LazyGrok debug mode. LazyGrok commands: /lg-debug, /debug."
metadata:
  short-description: Debugging alias
---

# debug

Load and follow the **debugging** skill exactly (same directory sibling `debugging/SKILL.md` and its `references/`). First line after load: treat as full hypothesis-driven OmO debug loop.
